name: 🚀 Semantic Release

on:
    push:
        branches:
            - develop # ou "master" selon ta convention

permissions:
    contents: write # pour créer des tags et releases
    issues: write # pour commenter les issues
    pull-requests: write # pour commenter les PR

jobs:
    release:
        name: Release
        runs-on: ubuntu-latest

        steps:
            - name: Checkout
              uses: actions/checkout@v4
              with:
                  fetch-depth: 0 # indispensable pour récupérer les tags

            - name: Setup Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: 20

            - name: Install dependencies
              run: npm install

            - name: Run Semantic Release
              env:
<<<<<<< HEAD
                  GITHUB_TOKEN: ${{ secrets.SEMANTIC }}
              run: npx semantic-release
=======
                  GITHUB_TOKEN: ${{ secrets.SEMANTIC}}
              run: npx semantic-release
>>>>>>> 638805bd7112aac15e2b403f1175a396b111c795
